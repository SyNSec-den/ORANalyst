//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:19
// Package resolver provides internal resolver-related functionality.
package resolver

//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:20
import (
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:20
	_go_fuzz_dep_ "go-fuzz-dep"
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:20
)
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:20
import (
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:20
	_atomic_ "sync/atomic"
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:20
)

import (
	"context"
	"sync"

	"google.golang.org/grpc/internal/serviceconfig"
	"google.golang.org/grpc/metadata"
	"google.golang.org/grpc/resolver"
)

// ConfigSelector controls what configuration to use for every RPC.
type ConfigSelector interface {
	// Selects the configuration for the RPC, or terminates it using the error.
	// This error will be converted by the gRPC library to a status error with
	// code UNKNOWN if it is not returned as a status error.
	SelectConfig(RPCInfo) (*RPCConfig, error)
}

// RPCInfo contains RPC information needed by a ConfigSelector.
type RPCInfo struct {
	// Context is the user's context for the RPC and contains headers and
	// application timeout.  It is passed for interception purposes and for
	// efficiency reasons.  SelectConfig should not be blocking.
	Context	context.Context
	Method	string	// i.e. "/Service/Method"
}

// RPCConfig describes the configuration to use for each RPC.
type RPCConfig struct {
	// The context to use for the remainder of the RPC; can pass info to LB
	// policy or affect timeout or metadata.
	Context		context.Context
	MethodConfig	serviceconfig.MethodConfig	// configuration to use for this RPC
	OnCommitted	func()				// Called when the RPC has been committed (retries no longer possible)
	Interceptor	ClientInterceptor
}

// ClientStream is the same as grpc.ClientStream, but defined here for circular
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:58
// dependency reasons.
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:60
type ClientStream interface {
	// Header returns the header metadata received from the server if there
	// is any. It blocks if the metadata is not ready to read.
	Header() (metadata.MD, error)
	// Trailer returns the trailer metadata from the server, if there is any.
	// It must only be called after stream.CloseAndRecv has returned, or
	// stream.Recv has returned a non-nil error (including io.EOF).
	Trailer() metadata.MD
	// CloseSend closes the send direction of the stream. It closes the stream
	// when non-nil error is met. It is also not safe to call CloseSend
	// concurrently with SendMsg.
	CloseSend() error
	// Context returns the context for this stream.
	//
	// It should not be called until after Header or RecvMsg has returned. Once
	// called, subsequent client-side retries are disabled.
	Context() context.Context
	// SendMsg is generally called by generated code. On error, SendMsg aborts
	// the stream. If the error was generated by the client, the status is
	// returned directly; otherwise, io.EOF is returned and the status of
	// the stream may be discovered using RecvMsg.
	//
	// SendMsg blocks until:
	//   - There is sufficient flow control to schedule m with the transport, or
	//   - The stream is done, or
	//   - The stream breaks.
	//
	// SendMsg does not wait until the message is received by the server. An
	// untimely stream closure may result in lost messages. To ensure delivery,
	// users should ensure the RPC completed successfully using RecvMsg.
	//
	// It is safe to have a goroutine calling SendMsg and another goroutine
	// calling RecvMsg on the same stream at the same time, but it is not safe
	// to call SendMsg on the same stream in different goroutines. It is also
	// not safe to call CloseSend concurrently with SendMsg.
	SendMsg(m interface{}) error
	// RecvMsg blocks until it receives a message into m or the stream is
	// done. It returns io.EOF when the stream completes successfully. On
	// any other error, the stream is aborted and the error contains the RPC
	// status.
	//
	// It is safe to have a goroutine calling SendMsg and another goroutine
	// calling RecvMsg on the same stream at the same time, but it is not
	// safe to call RecvMsg on the same stream in different goroutines.
	RecvMsg(m interface{}) error
}

// ClientInterceptor is an interceptor for gRPC client streams.
type ClientInterceptor interface {
	// NewStream produces a ClientStream for an RPC which may optionally use
	// the provided function to produce a stream for delegation.  Note:
	// RPCInfo.Context should not be used (will be nil).
	//
	// done is invoked when the RPC is finished using its connection, or could
	// not be assigned a connection.  RPC operations may still occur on
	// ClientStream after done is called, since the interceptor is invoked by
	// application-layer operations.  done must never be nil when called.
	NewStream(ctx context.Context, ri RPCInfo, done func(), newStream func(ctx context.Context, done func()) (ClientStream, error)) (ClientStream, error)
}

// ServerInterceptor is an interceptor for incoming RPC's on gRPC server side.
type ServerInterceptor interface {
	// AllowRPC checks if an incoming RPC is allowed to proceed based on
	// information about connection RPC was received on, and HTTP Headers. This
	// information will be piped into context.
	AllowRPC(ctx context.Context) error	// TODO: Make this a real interceptor for filters such as rate limiting.
}

type csKeyType string

const csKey = csKeyType("grpc.internal.resolver.configSelector")

// SetConfigSelector sets the config selector in state and returns the new
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:132
// state.
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:134
func SetConfigSelector(state resolver.State, cs ConfigSelector) resolver.State {
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:134
	_go_fuzz_dep_.CoverTab[68987]++
														state.Attributes = state.Attributes.WithValue(csKey, cs)
														return state
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:136
	// _ = "end of CoverTab[68987]"
}

// GetConfigSelector retrieves the config selector from state, if present, and
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:139
// returns it or nil if absent.
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:141
func GetConfigSelector(state resolver.State) ConfigSelector {
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:141
	_go_fuzz_dep_.CoverTab[68988]++
														cs, _ := state.Attributes.Value(csKey).(ConfigSelector)
														return cs
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:143
	// _ = "end of CoverTab[68988]"
}

// SafeConfigSelector allows for safe switching of ConfigSelector
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:146
// implementations such that previous values are guaranteed to not be in use
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:146
// when UpdateConfigSelector returns.
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:149
type SafeConfigSelector struct {
	mu	sync.RWMutex
	cs	ConfigSelector
}

// UpdateConfigSelector swaps to the provided ConfigSelector and blocks until
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:154
// all uses of the previous ConfigSelector have completed.
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:156
func (scs *SafeConfigSelector) UpdateConfigSelector(cs ConfigSelector) {
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:156
	_go_fuzz_dep_.CoverTab[68989]++
														scs.mu.Lock()
														defer scs.mu.Unlock()
														scs.cs = cs
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:159
	// _ = "end of CoverTab[68989]"
}

// SelectConfig defers to the current ConfigSelector in scs.
func (scs *SafeConfigSelector) SelectConfig(r RPCInfo) (*RPCConfig, error) {
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:163
	_go_fuzz_dep_.CoverTab[68990]++
														scs.mu.RLock()
														defer scs.mu.RUnlock()
														return scs.cs.SelectConfig(r)
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:166
	// _ = "end of CoverTab[68990]"
}

//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:167
var _ = _atomic_.LoadUint32(&_go_fuzz_dep_.NoUse)
//line /home/tianchang/go/pkg/mod/google.golang.org/grpc@v1.54.0/internal/resolver/config_selector.go:167
var _ = _go_fuzz_dep_.CoverTab
